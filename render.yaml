services:
  - type: web
    name: localchat-signaling
    env: node
    buildCommand: npm install
    startCommand: node simple-signaling-server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
